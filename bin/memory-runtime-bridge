#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd -- "${SCRIPT_DIR}/.." && pwd)"

exec node \
  --experimental-strip-types \
  --loader "${PROJECT_ROOT}/scripts/ts-local-loader.mjs" \
  "${PROJECT_ROOT}/scripts/memory-runtime-bridge.ts"
